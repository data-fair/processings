{
  "$id": "https://github.com/data-fair/processings/worker/config",
  "x-exports": [
    "types",
    "validate"
  ],
  "x-ajv": {
    "coerceTypes": "array"
  },
  "type": "object",
  "title": "Worker config",
  "additionalProperties": false,
  "required": [
    "cipherPassword",
    "dataFairAPIKey",
    "dataFairAdminMode",
    "dataFairUrl",
    "defaultLimits",
    "mails",
    "maxFailures",
    "maxFailuresCooldown",
    "mongoUrl",
    "locks",
    "observer",
    "privateRegistryUrl",
    "runsRetention",
    "worker",
    "secretKeys",
    "upgradeRoot"
  ],
  "properties": {
    "cipherPassword": {
      "type": "string"
    },
    "dataDir": {
      "type": ["string", "null"],
      "description": "Optional. When set, the legacy plugins volume at <dataDir>/plugins is read by the v6.0 boot migration. Drops with v7.0."
    },
    "tmpDir": {
      "type": "string"
    },
    "dataFairAPIKey": {
      "type": "string"
    },
    "dataFairAdminMode": {
      "type": "boolean"
    },
    "dataFairUrl": {
      "type": "string",
      "pattern": "^https?://"
    },
    "defaultLimits" : {
      "type": "object",
      "properties": {
        "processingsSeconds": {
          "type": "number"
        }
      }
    },
    "getFromPrivateDataFairUrl": {
      "type": "boolean"
    },
    "mails": {
      "type": "object",
      "properties": {
        "transport": {
          "type": "object"
        }
      }
    },
    "maxFailures": {
      "type": "number",
      "description": "Number of failures before a processing becomes inactive"
    },
    "maxFailuresCooldown": {
      "type": "number",
      "description": "Minimum time in hours between first failure and last failure to disable a processing. Set to 0 to disable cooldown."
    },
    "mongoUrl": {
      "type": "string"
    },
    "privateRegistryUrl": {
      "type": "string",
      "pattern": "^https?://",
      "description": "Internal URL used by the worker to call registry server-to-server."
    },
    "secretKeys": {
      "type": "object",
      "required": ["registry"],
      "properties": {
        "events": {
          "type": ["string", "null"]
        },
        "registry": {
          "type": "string",
          "description": "x-secret-key shared with registry. Required for prepare-hook downloads and the v6.0 first-boot migration."
        }
      }
    },
    "privateEventsUrl": {
      "type": "string"
    },
    "locks": {
      "type": "object",
      "required": [
        "ttl"
      ],
      "properties": {
        "ttl": {
          "type": "number"
        }
      }
    },
    "observer": {
      "type": "object",
      "properties": {
        "active": {
          "type": "boolean"
        },
        "port": {
          "type": "number"
        }
      }
    },
    "privateDataFairUrl": {
      "type": "string"
    },
    "runsRetention": {
      "type": "number",
      "description": "Number of run logs to keep"
    },
    "worker": {
      "type": "object",
      "required": [
        "interval",
        "inactiveInterval",
        "inactivityDelay",
        "killInterval",
        "concurrency",
        "gracePeriod"
      ],
      "properties": {
        "interval": {
          "type": "number"
        },
        "inactiveInterval": {
          "type": "number"
        },
        "inactivityDelay": {
          "type": "number"
        },
        "killInterval": {
          "type": "number"
        },
        "concurrency": {
          "type": "number"
        },
        "gracePeriod": {
          "type": "number"
        }
      }
    },
    "upgradeRoot": {
      "type": "string"
    },
    "util": {},
    "get": {},
    "has": {}
  }
}
